{
  "name": "@ai-toolkit/workflow-engine",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "clean": "tsc --build --clean",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ai-toolkit/shared": "^1.0.0",
    "@ai-toolkit/core": "^1.0.0",
    "bullmq": "^5.0.0",
    "ioredis": "^5.3.0",
    "cron": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "@types/cron": "^2.4.0"
  }
}
