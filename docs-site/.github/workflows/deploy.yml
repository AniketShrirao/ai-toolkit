name: Deploy Documentation Site

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: docs-site/package-lock.json
        
    - name: Install dependencies
      working-directory: docs-site
      run: npm ci
      
    - name: Type check
      working-directory: docs-site
      run: npm run type-check
      
    - name: Lint
      working-directory: docs-site
      run: npm run lint
      
    - name: Build
      working-directory: docs-site
      run: npm run build:static
      env:
        NEXT_PUBLIC_SITE_URL: ${{ secrets.SITE_URL || 'https://your-domain.com' }}
        NEXT_PUBLIC_SITE_NAME: 'AI Toolkit Documentation'
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: documentation-site
        path: docs-site/out/
        
    # Deploy to GitHub Pages (uncomment if using GitHub Pages)
    # - name: Deploy to GitHub Pages
    #   if: github.ref == 'refs/heads/main'
    #   uses: peaceiris/actions-gh-pages@v3
    #   with:
    #     github_token: ${{ secrets.GITHUB_TOKEN }}
    #     publish_dir: docs-site/out
    #     cname: your-custom-domain.com  # Optional: add your custom domain
        
    # Deploy to Netlify (uncomment if using Netlify)
    # - name: Deploy to Netlify
    #   if: github.ref == 'refs/heads/main'
    #   uses: nwtgck/actions-netlify@v2.0
    #   with:
    #     publish-dir: docs-site/out
    #     production-branch: main
    #     github-token: ${{ secrets.GITHUB_TOKEN }}
    #     deploy-message: "Deploy from GitHub Actions"
    #   env:
    #     NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
    #     NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        
    # Deploy to Vercel (uncomment if using Vercel)
    # - name: Deploy to Vercel
    #   if: github.ref == 'refs/heads/main'
    #   uses: amondnet/vercel-action@v25
    #   with:
    #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
    #     vercel-org-id: ${{ secrets.ORG_ID }}
    #     vercel-project-id: ${{ secrets.PROJECT_ID }}
    #     working-directory: docs-site